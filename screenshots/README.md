# Screenshots

`01-connect.png` (the landing / Connect Freighter screen) is included and referenced by the main README.

Optional: to also show the connected / balance / send views, run the app (see Quick Start), connect Freighter on Testnet, and save a snip here as `02-connected.png`, then add an image reference in the main README. The README's "Verified on testnet" section already provides independent on-chain proof that the send flow works.
